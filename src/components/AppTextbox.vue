<template>
  <textarea />
</template>

<style scoped>
textarea {
  min-width: 0;
  padding: 10px;
  font: inherit;
  border: var(--border);
  border-radius: var(--rounded);
  box-shadow: var(--shadow);
}
</style>
